1. How to run project:
	right-click on the project name -> click 'Run Maven' -> choose 'Run first project' from list of Goal
	
2. Instruction for UI testing 
	Before you start writing tests, you should:
		a) install Chrome browser
		b) install Firefox browser 
		c) install Selenium IDE extension for Firefox browser
